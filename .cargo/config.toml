[env]
# Use bundled bindings to avoid bindgen issues with _IO_FILE struct size
WHISPER_DONT_GENERATE_BINDINGS = "1"

[build]
rustflags = ["-C", "link-arg=-fuse-ld=mold"]

[rust-analyzer]
cargo.features = ["full"]
